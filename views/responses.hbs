<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Response Details</title>

  <link rel="icon" href="/logo.jpeg" type="image/icon type">

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  
  <!-- Font Awesome for Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

  <style>
    * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
    font-family: Arial, sans-serif;
}
    body {
      background-image: url("/Home1.jpg");
      background-repeat: no-repeat;
      background-position: center center;
      background-attachment: fixed;
      background-size: cover;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      
    }

    .container {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      width: 100%;
      max-width: 900px;
      background: rgba(255, 255, 255, 0.1); /* Light translucent background */
      padding: 20px;
      border-radius: 10px;
      backdrop-filter: blur(5px);
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
    }

    h1 {
      font-size: 32px;
      margin-bottom: 20px;
      color: white; /* Changed to white for better visibility */
      text-align: center;
    }

    .response-details {
      margin-bottom: 20px;
      overflow-y: auto;
      max-height: 290px;
      width: 100%;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }
      .response-details p {
      font-size: 18px;
      margin-bottom: 10px;
      color: #fff; /* Changed to white for readability against background */
      text-align: center; /* Center the text */
    }

    .response {
      margin-bottom: 20px;
      padding: 20px;
      background: rgba(0, 0, 0, 0.5); /* Darker background for contrast */
      border-radius: 8px;
      color: white;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    .btn {
      display: inline-block;
      padding: 10px 20px;
      background-color: #6c5b7b;
      color: #fff;
      border: none;
      border-radius: 5px;
      text-decoration: none;
      transition: background-color 0.3s ease;
      cursor: pointer;
      margin: 5px 0;
      font-family: 'Roboto', sans-serif;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .btn:hover {
      background-color: #54466d;
    }

    .action-buttons {
      display: flex;
      justify-content: center;
      margin-top: 10px;
    }

    .button-group {
      display: flex;
      gap: 15px; /* Space between buttons */
      justify-content: center;
    }

    form {
      display: inline;
    }

  </style>
</head>
<body>
  <div class="container">
    <h1><i class="fas fa-comments"></i> Response Details</h1>
    <div class="response-details">
      {{#if responses.length}}
        {{#each responses}}
          <div class="response">
            <p><i class="fas fa-user"></i><strong>Username:</strong> {{responder.username}}</p>
            <p><i class="fab fa-github"></i><strong>GitHub ID:</strong> {{responder.github_id}}</p>
            <div class="action-buttons">
              <form id="convo" action="/chat/start?username={{responder.username}}&queryId={{issue}}&resId={{_id}}" method="POST">
                <input type="hidden" id="queryId" name="queryId" value="{{issue}}" readonly required>
                <input type="hidden" id="username" name="username" value="{{responder.username}}" readonly required>
                <input type="hidden" id="response" name="response" value="{{_id}}" readonly required>
                {{#if approved}}
                {{else}}
                <button type="submit" class="btn"><i class="fas fa-check-circle"></i> Approve</button>
                {{/if}}
              </form>
            </div>
          </div>
        {{/each}}
      {{else}}
        <p>No responses found.</p>
      {{/if}}
    </div>
    <div class="button-group">
    <a href="/query/list" class="btn"><i class="fas fa-arrow-left"></i>Back to Queries</a>
    <a href="/api/user" class="btn"><i class="fas fa-tachometer-alt"></i>Back to Dashboard</a>
    </div>
  </div>
</body>
</html>
