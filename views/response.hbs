<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create Response</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background: linear-gradient(135deg, #6c5b7b, #000000);
    }

    .container {
      background-color: #f0e9ff;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
      text-align: center;
      max-width: 400px;
      width: 100%;
    }

    h1 {
      font-size: 28px;
      margin-bottom: 20px;
      color: #333;
    }

    .form-group {
      margin-bottom: 20px;
      text-align: left;
    }

    .form-group label {
      display: block;
      font-size: 16px;
      margin-bottom: 5px;
      color: #666;
    }

    .form-group input,
    .form-group textarea {
      width: 95%;
      padding: 10px;
      font-size: 16px;
      border-radius: 5px;
      border: 1px solid #ccc;
    }

    .btn {
      padding: 12px 24px;
      background-color: #6c5b7b;
      color: white;
      border: none;
      border-radius: 6px;
      font-size: 16px;
      cursor: pointer;
      text-decoration: none;
      display: inline-block;
      transition: background-color 0.3s ease;
    }

    .btn:hover {
      background-color: #54466d;
    }

    #home-btn {
      margin-top: 15px;
    }

    #home-btn a {
      color: white;
      text-decoration: none;
    }

    .error-message {
      color: red;
      font-size: 14px;
      margin-top: 10px;
    }
  </style>
  <script>
    function validateForm() {
      var githubId = document.getElementById("github_id").value;

      var githubIdRegex = /^[a-zA-Z0-9-]{1,39}$/;
      if (!githubIdRegex.test(githubId)) {
        document.getElementById("error-message").innerText = "Invalid GitHub ID.";
        return false;
      }

      document.getElementById("error-message").innerText = "";
      return true;
    }
  </script>
</head>
<body>
  <div class="container">
    <h1>Response</h1>
    <form action="/query/respond" method="POST" onsubmit="return validateForm()">
      <div class="form-group">
        <label for="github_id">Your GitHub ID:</label>
        <input type="text" id="github_id" name="github_id" required>
      </div>
      <input type="hidden" name="issue_id" value="{{issue_id}}">
      <input type="hidden" name="creator" value="{{creator}}">
      <input type="hidden" name="id" value="{{_id}}">
      <input type="submit" value="Submit" class="btn">
    </form>
    <button type="button" id="home-btn" class="btn"><a href="/">Go To Homepage</a></button>
    <p id="error-message" class="error-message"></p>
  </div>
</body>
</html>
