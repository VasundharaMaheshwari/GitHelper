<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Opened Queries</title>

  <style>
    body {
      font-family: 'Roboto', sans-serif;
      margin: 0;
      padding: 0;
      background-image: url("Home1.jpg");
      background-repeat: no-repeat;
      background-position: center center;
      background-attachment: fixed;
      background-size: cover;
      color: #f0f0f0;
    }

    .container {
      max-width: 900px;
      margin: 60px auto;
      padding: 30px;
      border-radius: 30px;
      background-color: rgba(9, 23, 58, 0.8); /* Adjusted opacity */
      box-shadow: 3px 6px 15px rgba(255, 255, 255, 0.2);
      animation: fadeIn 1.2s ease-in-out;
      backdrop-filter: blur(0px); /* No blur behind the container */
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: scale(0.95); }
      to { opacity: 1; transform: scale(1); }
    }

    h1 {
      font-size: 33px;
      margin-bottom: 24px;
      text-align: center;
      color: #dbdada;
      animation: slideDown 1.2s ease-in-out;
    }

    @keyframes slideDown {
      from { transform: translateY(-30px); }
      to { transform: translateY(0); }
    }

    .issue {
      padding: 20px;
      margin-bottom: 20px;
      border-radius: 12px;
      background-color: rgba(23, 49, 101, 0.9);
      box-shadow: 6px 6px 12px rgba(71, 71, 71, 0.2);
      transition: transform 0.6s ease-in-out;
    }

    .issue:hover {
      transform: translateY(-6px);
      background-color: rgba(18, 37, 81, 0.9);
    }

    .issue h2 {
      font-size: 24px;
      margin-bottom: 12px;
      color: #9f9d9d;
    }

    .issue p {
      font-size: 16px;
      margin-bottom: 6px;
      color: #dbdada;
    }

    .issue span {
      font-weight: bold;
      color: #d1d1d1;
    }

    .no-issues {
      text-align: center;
      font-size: 21px;
      color: #b5b5b5;
    }

    .btn {
      display: inline-block;
      padding: 10px 20px;
      background-image: linear-gradient(45deg, #6a11cb, #2575fc);
      color: #fff;
      border: none;
      border-radius: 6px;
      text-decoration: none;
      font-weight: bold;
      cursor: pointer;
      transition: background-color 0.3s ease, transform 0.3s ease;
    }

    .btn:hover {
      transform: scale(1.05);
      background-image: linear-gradient(45deg, #5d09b1, #1f65e3);
    }

    .navbar {
      display: flex;
      justify-content: right;
      padding: 15px;
      border-radius: 10px;
      margin-bottom: 20px;
    }

    .navbar a {
      color: #fff;
      text-decoration: none;
      margin: 0 10px;
      padding: 12px 25px;
      font-weight: bold;
      border-radius: 25px;
      background-image: linear-gradient(45deg, #6a11cb, #2575fc);;
      transition: transform 0.3s ease, background-image 0.3s ease;
      font-size: 16px;
    }

    .navbar a:hover {
      background-image: linear-gradient(45deg, #5d09b1, #1f65e3);
      transform: scale(1.1);
    }

    .repo-link {
      color: #ffdc47;
      text-decoration: none;
      font-weight: bold;
      transition: color 0.3s ease;
    }

    .repo-link:hover {
      color: #ffb300;
    }

    @media (max-width: 768px) {
      .container {
        padding: 15px;
      }

      .navbar a {
        padding: 10px 15px;
        font-size: 14px;
      }

      h1 {
        font-size: 28px;
      }
    }
  </style>
</head>
<body>
  <div class="navbar">
    <a href="/error/faq">FAQ</a>
    {{#if user}}
    <a href="/api/user">Profile</a>
    {{else}}
    <a href="/api/register">Register</a>
    <a href="/api/login">Login</a>
    {{/if}}
  </div>

  <div class="container">
    <h1>Current Open Queries</h1>
    {{#if issues.length}}
      {{#each issues}}
        <div class="issue">
          <h2>{{title}}</h2>
          <p><span>Username:</span> {{username}}</p>
          <p><span>Skill Set:</span> {{skillset}}</p>
          <p><span>GitHub ID:</span> {{github_id}}</p>
          <p><span>Repository Link:</span> <a href={{repo_link}} class="repo-link" target="_blank">{{repo_link}}</a></p>
          <form action="/home/view" method="GET">
            <input type="hidden" name="_id" value={{_id}} required readonly hidden>
            <br>
            <button type="submit" class="btn">View Query</button>
          </form>
        </div>
      {{/each}}
    {{else}}
      <p class="no-issues">No open queries found.</p>
    {{/if}}
  </div>
</body>
</html>
