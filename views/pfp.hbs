<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile - Git Helper</title>
  <link rel="icon" href="/logo.jpeg" type="image/icon type">
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  <!-- Font Awesome for Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <!-- Animate.css -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Roboto', sans-serif;
    }

    body {
      background: url("/Home1.jpg") no-repeat center center fixed;
      background-size: cover;
      color: white;
      overflow-x: hidden;
      animation: fadeInBody 0.3s ease;
    }

    @keyframes fadeInBody {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    /* Navbar */
    .navbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 15px 30px;
      background: transparent;
      position: fixed;
      width: 100%;
      top: 0;
      z-index: 1000;
    }

    .logo-section {
      display: flex;
      align-items: center;
    }

    .logo-section img {
      height: 40px;
      margin-right: 10px;
      transition: transform 0.3s ease;
    }

    .logo-section img:hover {
      transform: rotate(10deg);
    }

    .logo-section span {
      font-size: 24px;
      font-weight: bold;
      letter-spacing: 2px;
      color: #fff;
      text-shadow: 0px 0px 5px rgba(255, 255, 255, 0.8);
    }

    /* Navbar buttons */
    .buttons {
      display: flex;
      gap: 15px;
    }

    .buttons a {
      display: inline-block;
      padding: 10px 20px;
      margin-left: 15px;
      background-image: linear-gradient(45deg, #6a11cb, #2575fc);
      color: #fff;
      text-decoration: none;
      border-radius: 25px;
      font-size: 16px;
      font-weight: bold;
      transition: background-image 0.3s ease, transform 0.3s ease;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
      position: relative;
    }

    .buttons a::after {
      content: '';
      position: absolute;
      width: 0%;
      height: 2px;
      bottom: -5px;
      left: 0;
      background-color: #fff;
      transition: width 0.3s;
    }

    .buttons a:hover::after {
      width: 100%;
    }

    .buttons a:hover {
      background-image: linear-gradient(45deg, #5d09b1, #1f65e3);
      transform: scale(1.05);
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.5);
    }

    .container {
      max-width: 1300px;
      margin: 130px auto 50px;
      padding: 30px;
      display: flex;
      flex-wrap: wrap;
      gap: 40px;
      justify-content: center;
    }

    .left-section,
    .right-section {
      animation: fadeUp 1s ease forwards;
      opacity: 0;
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(14px);
      border-radius: 20px;
      box-shadow: 0 12px 32px rgba(0, 0, 0, 0.4);
      padding: 30px;
      position: relative;
    }

    .left-section {
      flex: 0 0 32%;
      text-align: center;
    }

    .right-section {
      flex: 0 0 62%;
    }

    @keyframes fadeUp {
      0% {
        opacity: 0;
        transform: translateY(30px);
      }

      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .profile-picture {
      width: 160px;
      height: 160px;
      border-radius: 50%;
      object-fit: cover;
      margin-bottom: 20px;
      border: 3px solid #fff;
      transition: all 0.4s ease;
      box-shadow: 0 0 20px rgba(255, 255, 255, 0.4);
    }

    .profile-picture:hover {
      transform: scale(1.1);
      border-color: #6a11cb;
      box-shadow: 0 0 25px rgba(106, 17, 203, 0.7);
    }

    .user-details h3 {
      font-size: 20px;
      margin: 10px 0;
      text-shadow: 1px 1px 6px rgba(0, 0, 0, 0.6);
    }

    .history-title {
      font-size: 24px;
      font-weight: bold;
      margin-bottom: 20px;
      color: #fff;
      text-shadow: 1px 1px 8px rgba(0, 0, 0, 0.5);
      border-left: 4px solid #ff8a00;
      padding-left: 12px;
    }

    .project-list {
      list-style: none;
    }

    .project-item {
      background: rgba(0, 0, 0, 0.6);
      padding: 16px;
      margin-bottom: 15px;
      border-radius: 12px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      transition: 0.3s ease;
      border: 1px solid transparent;
    }

    .project-item:hover {
      background: rgba(0, 0, 0, 0.85);
      border-color: #ff8a00;
      box-shadow: 0 0 12px rgba(255, 138, 0, 0.4);
    }

    .project-name {
      font-size: 18px;
      font-weight: 600;
      margin-bottom: 5px;
    }

    .project-description {
      font-size: 14px;
      color: #ccc;
    }

    .repo-link {
      color: #ff8a00;
      text-decoration: none;
      font-weight: bold;
      transition: all 0.3s ease;
      font-size: 15px;
    }

    .repo-link:hover {
      text-decoration: underline;
      color: #ffc107;
    }
  </style>
</head>

<body>
  <!-- Navbar -->
  <div class="navbar">
    <div class="logo-section">
      <a href="/"><img src="/logo.jpeg" alt="Git Helper Logo"></a>
      <span>Git Helper</span>
    </div>
    <div class="buttons">
      <a href="/"><i class="fas fa-home"></i> Home</a>
      <a href="/error/leaderboard"><i class="fas fa-arrow-left"></i> Back</a>
    </div>
  </div>
  <!-- Main Container -->
  <div class="container">
    <!-- Left - Profile Card -->
    <div class="left-section glass-box animate-fade-up">
      <img src="/pfp.jpeg" alt="Default Profile Picture" class="profile-picture">
      <div class="user-details">
        <h3><strong>Username:</strong> {{username}}</h3>
        <h3><strong>Email:</strong> {{emailAddress}}</h3>
        <h3><strong>GitHub ID:</strong> {{githubId}}</h3>
      </div>
    </div>
    <!-- Right - Project History -->
    <div class="right-section glass-box animate-fade-up delay-1">
      <p class="history-title">📜 Project History</p>
      {{#if projects.length}}
        <ul class="project-list">
          {{#each projects}}
            <li class="project-item">
              <div>
                <span class="project-name">Assigned By: {{this.name}}</span>
                <p class="project-description">Work Done: {{this.description}}</p>
                {{#if project.unavailable}}
                  <strong>
                    <p>This repository may not be available to view anymore.</p>
                  </strong>
                {{/if}}
              </div>
              <a class="repo-link" href="{{this.repoLink}}" target="_blank" title="Open GitHub Repo">
                <i class="fas fa-external-link-alt"></i> View Repo
              </a>
            </li>
          {{/each}}
        </ul>
      {{else}}
        <p class="no-projects">No projects found.</p>
      {{/if}}
    </div>
  </div>
</body>

</html>