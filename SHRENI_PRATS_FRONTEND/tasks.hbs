<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Task Tracking</title>
  <link rel="icon" href="/logo.jpeg" type="image/icon type">
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Roboto', sans-serif;
    }

    body {
      background-image: url("/1329272.png");
      background-repeat: no-repeat;
      background-position: center center;
      background-attachment: fixed;
      background-size: cover;
      height: 100vh;
      color: white;
      overflow-x: hidden;
      scroll-behavior: smooth;
    }

    .navbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 15px 30px;
      background: transparent;
      position: fixed;
      width: 100%;
      top: 0;
      z-index: 1000;
    }

    .logo-section {
      display: flex;
      align-items: center;
    }

    .logo-section img {
      height: 40px;
      margin-right: 10px;
    }

    .logo-section span {
      font-size: 24px;
      font-weight: bold;
      color: #fff;
      text-shadow: 0 0 5px rgba(255, 255, 255, 0.8);
    }

    .container {
      width: 90%;
      max-width: 1100px;
      margin: 120px auto;
      padding: 25px;
      background: rgba(255, 255, 255, 0.1);
      border-radius: 20px;
      backdrop-filter: blur(15px);
      box-shadow: 0 12px 35px rgba(0, 0, 0, 0.6);
    }

    h1 {
      font-size: 32px;
      margin-bottom: 20px;
      text-align: center;
    }

    /* Tabs Section */
    .tab-container {
      display: flex;
      justify-content: center;
      gap: 15px;
      margin-bottom: 20px;
    }

    .tab {
      padding: 12px 20px;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      border-radius: 25px;
      background: rgba(0, 0, 0, 0.6);
      border: 2px solid transparent;
      color: white;
      transition: all 0.3s ease;
    }

    .tab:hover {
      background: rgba(255, 255, 255, 0.2);
    }

    .tab.active {
      border: 2px solid #ff8a00;
      background: rgba(255, 255, 255, 0.2);
    }

    .task-list {
      display: none;
    }

    .task-list.active {
      display: block;
    }

    .task {
      font-size: 16px;
      margin-bottom: 20px;
      padding: 20px;
      border-bottom: 2px solid rgba(255, 255, 255, 0.2);
      background: rgba(0, 0, 0, 0.6);
      border-radius: 15px;
      color: white;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.4);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .task:hover {
      transform: scale(1.02);
      background-color: rgba(0, 0, 0, 0.8);
    }

    .task-actions {
      margin-top: 15px;
      display: flex;
      gap: 10px;
    }

    .accept-btn, .reject-btn {
      padding: 8px 15px;
      font-size: 14px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-weight: bold;
    }

    .accept-btn {
      background-color: #28a745;
      color: white;
    }

    .reject-btn {
      background-color: #dc3545;
      color: white;
    }

    .accept-btn:hover {
      background-color: #218838;
    }

    .reject-btn:hover {
      background-color: #c82333;
    }


    .no-issues {
      text-align: center;
      font-size: 20px;
      margin: 20px 0;
    }

    .btn {
      display: block;
      text-align: center;
      padding: 12px 24px;
      background-image: linear-gradient(45deg, #6a11cb, #2575fc);
      color: #fff;
      border-radius: 30px;
      text-decoration: none;
      margin: 20px auto;
      max-width: 200px;
    }

    .btn:hover {
      background-image: linear-gradient(45deg, #5d09b1, #1f65e3);
    }
  </style>
</head>

<body>

  <div class="navbar">
    <div class="logo-section">
      <a href="/"><img src="/logo.jpeg" alt="Git Helper Logo"></a>
      <span>Git Helper</span>
    </div>
  </div>

  <div class="container">
    <h1>Task Tracking</h1>

    <!-- Tabs for Filtering -->
    <div class="tab-container">
      <div class="tab active" data-category="todo">To Do</div>
      <div class="tab" data-category="in-progress">In Progress</div>
      <div class="tab" data-category="completed">Completed</div>
    </div>

    <!-- Task Lists -->
    <div id="todo" class="task-list active">
      {{#if tasks.todo.length}}
        {{#each tasks.todo}}
        <div class="task">
          <p><strong>📅 Assigned At:</strong> {{assigned_at}}</p>
          <p><strong>👤 Assigned By:</strong> {{assigned_by.username}} (GitHub: <a class="github-id" href="https://github.com/{{assigned_by.github_id}}" target="_blank"><i class="fas fa-external-link-alt"></i> {{assigned_by.github_id}}</a>)</p>
          <p><strong>📝 Task:</strong> {{description}}</p>
          <p><strong>🔗 Repository:</strong> <a class="github-id" href="{{repository_url}}" target="_blank"><i class="fas fa-external-link-alt"></i> Open Repo</a></p>
        </div>
        {{/each}}
      {{else}}
        <p class="no-issues">No tasks in To Do.</p>
      {{/if}}
    </div>

    <div id="in-progress" class="task-list">
      {{#if tasks.inProgress.length}}
        {{#each tasks.inProgress}}
        <div class="task">
          <p><strong>📅 Assigned At:</strong> {{assigned_at}}</p>
          <p><strong>👤 Assigned By:</strong> {{assigned_by.username}} (GitHub: <a class="github-id" href="https://github.com/{{assigned_by.github_id}}" target="_blank"><i class="fas fa-external-link-alt"></i> {{assigned_by.github_id}}</a>)</p>
          <p><strong>📝 Task:</strong> {{description}}</p>
          <p><strong>🔗 Repository:</strong> <a class="github-id" href="{{repository_url}}" target="_blank"><i class="fas fa-external-link-alt"></i> Open Repo</a></p>
        </div>
        {{/each}}
      {{else}}
        <p class="no-issues">No tasks in Progress.</p>
      {{/if}}
    </div>

    <div id="completed" class="task-list">
      {{#if tasks.completed.length}}
        {{#each tasks.completed}}
        <div class="task">
          <p><strong>📅 Assigned At:</strong> {{assigned_at}}</p>
          <p><strong>👤 Assigned By:</strong> {{assigned_by.username}} (GitHub: <a class="github-id" href="https://github.com/{{assigned_by.github_id}}" target="_blank"><i class="fas fa-external-link-alt"></i> {{assigned_by.github_id}}</a>)</p>
          <p><strong>📝 Task:</strong> {{description}}</p>
          <p><strong>🔗 Repository:</strong> <a class="github-id" href="{{repository_url}}" target="_blank"><i class="fas fa-external-link-alt"></i> Open Repo</a></p>
    
          <!-- Accept and Reject Buttons -->
          <div class="task-actions">
            <button class="accept-btn" data-task-id="{{id}}">Accept</button>
            <button class="reject-btn" data-task-id="{{id}}">Reject</button>
          </div>
        </div>
        {{/each}}
      {{else}}
        <p class="no-issues">No completed tasks.</p>
      {{/if}}
    </div>

    <a href="/api/user" class="btn">Back to Dashboard</a>
  </div>

  <script>
    document.querySelectorAll(".tab").forEach(tab => {
      tab.addEventListener("click", function () {
        document.querySelectorAll(".tab").forEach(t => t.classList.remove("active"));
        document.querySelectorAll(".task-list").forEach(list => list.classList.remove("active"));

        this.classList.add("active");
        document.getElementById(this.dataset.category).classList.add("active");
      });
    });

    document.querySelectorAll(".accept-btn").forEach(button => {
      button.addEventListener("click", function () {
        const taskId = this.getAttribute("data-task-id");
        alert(`Task ${taskId} accepted!`);
        // Add AJAX or fetch request here to update backend
      });
    });
    
    document.querySelectorAll(".reject-btn").forEach(button => {
      button.addEventListener("click", function () {
        const taskId = this.getAttribute("data-task-id");
        alert(`Task ${taskId} rejected!`);
        // Add AJAX or fetch request here to update backend
      });
    });

  </script>

</body>
</html>
