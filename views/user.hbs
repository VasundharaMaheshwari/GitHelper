<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Page</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background: linear-gradient(135deg, #6c5b7b, #000000);
    }

    .user-container {
      background-color: #f0e9ff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
      text-align: center;
      max-width: 400px;
      width: 100%;
    }

    h2 {
      font-size: 28px;
      margin-bottom: 20px;
      color: #333;
    }

    p {
      font-size: 16px;
      color: #666;
      margin-bottom: 20px;
    }

    button,
    #open-query-btn {
      padding: 12px 24px;
      background-color: #007bff;
      color: white;
      border: none;
      border-radius: 6px;
      font-size: 16px;
      cursor: pointer;
      text-decoration: none; 
      display: inline-block;
    }

    button:hover,
    #open-query-btn:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <div class="user-container">
    <h2>Welcome, {{username}}!</h2>
    <p>Email: {{email}}</p>
    <p>This is your user page. You can access your account information here.</p>
    <div class="button-container">
      <form id="query-form" action="/query/create" method="POST">
      <button type="button" id="open-query-btn">Open Query</button>
      <input type="text" name="username" id="username" value={{username}} required readonly hidden>
    </form>
    <br>
      <button id="logout-btn">Logout</button>
    </div>
  </div>
  <script>
    document.getElementById("logout-btn").addEventListener("click", function() {
      window.location.href = "/api/login"; 
    });
    document.getElementById("open-query-btn").addEventListener("click", function() {
      document.getElementById("query-form").submit();
    });
  </script>
</body>
</html>
