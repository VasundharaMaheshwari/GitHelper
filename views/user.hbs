<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Page</title>

  <link rel="icon" href="/logo.jpeg" type="image/icon type">

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  
  <!-- Font Awesome for Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Roboto', sans-serif;
    }

    body {
      background-image: url("/Home1.jpg");
      background-repeat: no-repeat;
      background-position: center center;
      background-attachment: fixed;
      background-size: cover;
      height: 100vh;
    }

    .container {
      width: 100%;
      max-width: 1200px;
      margin: 20px auto;
      padding: 0 20px;
    }

    /* Navbar with GitHelper on the left and buttons on the right */
    .navbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      position: absolute;
      top: 20px;
      width: 100%;
      padding: 0 20px;
    }

    /* GitHelper logo and text */
    .logo-section {
      display: flex;
      align-items: center;
      color: white;
    }

    .logo-section img {
      height: 40px;
      margin-right: 10px;
      transition: transform 0.3s ease;
    }

    .logo-section img:hover {
      transform: rotate(10deg);
    }

    .logo-section span {
      font-size: 18px;
      font-weight: bold;
    }

    /* Navbar buttons (Home and Logout) on the right */
    .navbar .buttons {
      display: flex;
      gap: 15px;
    }

    .navbar a {
      color: white;
      text-decoration: none;
      background: linear-gradient(45deg, #6a11cb, #2575fc);
      padding: 10px 20px;
      border-radius: 20px;
      transition: background 0.3s ease;
    }

    .navbar a:hover {
      background: linear-gradient(45deg, #5d09b1, #1f65e3);
    }

    /* Welcome Line below Navbar */
    .welcome-line {
      text-align: center;
      margin-top: 80px; /* Pushes the welcome line below the navbar */
      margin-bottom: 30px;
    }

    .welcome-line h2 {
      font-size: 36px;
      color: white;
    }

    /* Full Width Layout - Left Side for Buttons, Right Side for Details */
    .content-container {
      display: flex;
      width: 100%;
      margin-top: 20px;
    }

    /* Left Section - Buttons */
    .left-section {
      flex: 0 0 25%;
      padding: 20px;
      background: rgba(255, 255, 255, 0.15);
      border-radius: 15px;
      backdrop-filter: blur(8px);
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.4);
      margin-right: 20px;
    }

    .button-container {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    .button-container a {
      text-align: left;
      background: linear-gradient(45deg, #6a11cb, #2575fc);
      color: white;
      padding: 12px;
      border-radius: 8px;
      text-decoration: none;
      transition: background 0.3s ease;
    }

    .button-container a:hover {
      background: linear-gradient(45deg, #5d09b1, #1f65e3);
    }

    /* Right Section - User Details */
    .right-section {
      flex: 0 0 75%;
      padding: 20px;
      background: rgba(255, 255, 255, 0.15);
      border-radius: 15px;
      backdrop-filter: blur(8px);
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.4);
    }

    .user-details {
      color: white;
    }

    .user-details h2 {
      font-size: 28px;
      margin-bottom: 15px;
    }

    .user-details p {
      font-size: 18px;
      margin-bottom: 10px;
    }
    
  </style>
</head>
<body>
  <!-- Navbar with GitHelper logo on the left and Home/Logout buttons on the right -->
  <div class="navbar">
    <!-- Left: GitHelper Logo -->
    <div class="logo-section">
      <a href="/"><img src="/logo.jpeg" alt="Git Helper Logo"> Git Helper </a>
    </div>
    
    <!-- Right: Home and Logout Buttons -->
    <div class="buttons">
      <a href="/"><i class="fas fa-home"></i> Go To Home</a>
      <a href="/home/logout"><i class="fas fa-sign-out-alt"></i> Proceed To Logout</a>
    </div>
  </div>

  <!-- Main Container -->
  <div class="container">
    <!-- Welcome Line -->
    <div class="welcome-line">
      <h2>Welcome, {{username}}!</h2>
    </div>

    <!-- Content Container: Left Buttons and Right User Details -->
    <div class="content-container">
      <!-- Left Section: Buttons -->
      <div class="left-section">
        <div class="button-container">
          <a href="/query/create"><i class="fas fa-plus-circle"></i> Open Query</a>
          <a href="/query/list"><i class="fas fa-list"></i> Show Queries</a>
          <a href="/chat/list"><i class="fas fa-comments"></i> Show Chats</a>
        </div>
      </div>

      <!-- Right Section: User Details -->
      <div class="right-section">
        <div class="user-details">
          <h2>User Information</h2>
          <p>Email: {{email}}</p>
          <p>Contact: {{contact}}</p>
          <p>GitHub ID: {{githubID}}</p>
          <p>Other details...</p>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
