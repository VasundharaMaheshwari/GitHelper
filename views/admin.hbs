<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Page</title>

  <link rel="icon" href="/logo.jpeg" type="image/icon type">

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  
  <!-- Font Awesome for Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  
  <style>
    /* Universal reset and box-sizing */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Roboto', sans-serif;
    }

    /* Body background and layout */
    body {
      background-image: url("/1329272.png");
      background-repeat: no-repeat;
      background-position: center center;
      background-attachment: fixed;
      background-size: cover;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    /* Admin container styling */
    .admin-container {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      width: 100%;
      max-width: 600px;
      padding: 40px;
      background: rgba(255, 255, 255, 0.1);
      border-radius: 10px;
      backdrop-filter: blur(8px);
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
      text-align: center;
    }

    h2 {
      font-size: 36px;
      color: white;
      margin-bottom: 20px;
    }

    p {
      font-size: 16px;
      color: #e0e0e0;
      margin-bottom: 30px;
    }

    /* Button styling */
    button {
      padding: 12px 24px;
      background-color: #dc3545;
      color: white;
      border: none;
      border-radius: 6px;
      font-size: 16px;
      cursor: pointer;
      transition: background-color 0.3s ease;
      margin-bottom: 15px;
      width: 100%; /* Full width for better appearance */
      display: flex;
      align-items: center;
      justify-content: center;
    }

    /* Button hover effect */
    button:hover {
      background-color: #c82333;
    }

    /* Icon styling within the buttons */
    button i {
      margin-right: 8px;
    }

    /* Responsive layout for smaller screens */
    @media (max-width: 600px) {
      .admin-container {
        padding: 30px;
      }

      h2 {
        font-size: 28px;
      }

      button {
        font-size: 14px;
        padding: 10px 20px;
      }
    }
  </style>
</head>
<body>
  <div class="admin-container">
    <h2>Welcome, Admin!</h2>
    <p>This is the admin page. You have access to privileged information here.</p>
    <!-- Buttons with icons for admin actions -->
    <button id="home-btn"><i class="fas fa-list"></i>List of Queries</button>
    <button id="mod-btn"><i class="fas fa-users"></i>List of Users</button>
    <button id="logout-btn"><i class="fas fa-sign-out-alt"></i>Logout</button>
  </div>
  
  <script>
    document.getElementById("logout-btn").addEventListener("click", function() {
  window.location.href = "/home/logout"; 
});
    document.getElementById("mod-btn").addEventListener("click", function() {
  window.location.href = "/admin/userlist"; 
});
    document.getElementById("home-btn").addEventListener("click", function() {
  window.location.href = "/"; 
});
  </script>
</body>
</html>
