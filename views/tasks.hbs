<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Task Tracking</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background: linear-gradient(135deg, #6c5b7b, #000000);
    }

    .container {
      background-color: #f0e9ff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
      text-align: center;
      max-width: 800px;
      width: 100%;
    }

    h1 {
      font-size: 32px;
      margin-bottom: 20px;
      color: #6c5b7b;
    }

    .task-details {
      margin-bottom: 20px;
      overflow-y: scroll;
      max-height: 290px;
    }

    .task-details p {
      font-size: 18px;
      margin-bottom: 10px;
      color: #333;
    }

    .btn {
      display: inline-block;
      padding: 10px 20px;
      background-color: #6c5b7b;
      color: #fff;
      border: none;
      border-radius: 5px;
      text-decoration: none;
      transition: background-color 0.3s ease;
      cursor: pointer;
      margin: 5px 0;
    }

    .btn:hover {
      background-color: #54466d;
    }

    .task {
      margin-bottom: 20px;
      padding: 20px;
      background-color: #ffffff;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    .action-buttons {
      display: flex;
      justify-content: center;
      margin-top: 10px;
    }

    form {
      display: inline;
    }

    .github-id {
      color: #333;
      text-decoration: none;
      position: relative;
    }

    .github-id:hover {
      text-decoration: underline;
      color: #6c5b7b;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Task Tracking</h1>
    <div class="task-details">
      {{#if tasks.length}}
        {{#each tasks}}
          <div class="task">
            <p><strong>Assigned at:</strong> {{assigned_at}}</p>
            <p><strong>Assigned by:</strong> {{assigned_by.username}} (GitHub: <a class="github-id" href="https://github.com/{{assigned_by.github_id}}" target="_blank">{{assigned_by.github_id}}</a>)</p>
            <p><strong>Assigned to:</strong> {{assigned_to.username}} (GitHub: <a class="github-id" href="https://github.com/{{assigned_to.github_id}}" target="_blank">{{assigned_to.github_id}}</a>)</p>
            <p><strong>Task:</strong> {{description}}</p>
          </div>
        {{/each}}
      {{else}}
        <p>No tasks found.</p>
      {{/if}}
    </div>
    <a href="/api/user" class="btn">Back to Dashboard</a>
  </div>
</body>
</html>
