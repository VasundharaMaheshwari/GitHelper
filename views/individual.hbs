<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Query Details</title>

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  
  <!-- Font Awesome for Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

  <style>
    * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
    font-family: Arial, sans-serif;
}
    body {
      background-image: url("/Home1.jpg");
      background-repeat: no-repeat;
      background-position: center center;
      background-attachment: fixed;
      background-size: cover;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      
    }

    .container {
      background-color: rgba(240, 233, 255, 0.9); /* Semi-transparent background */
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
      text-align: center;
      max-width: 800px;
      width: 100%;
      backdrop-filter: blur(10px); /* Blur effect for a glassy look */
    }

    h1 {
      font-size: 32px;
      margin-bottom: 20px;
      color: #6c5b7b;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
    }

    .issue-details {
      margin-bottom: 20px;
    }

     .issue-details p {
      font-size: 18px;
      margin-bottom: 10px;
      color: #333;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .repo-link {
      color: #007bff;
      text-decoration: none;
      transition: color 0.3s ease;
    }

    .repo-link:hover {
      color: #0056b3;
      text-decoration: underline;
    }

    .btn-container {
      display: flex;
      justify-content: center;
      gap: 10px;
      margin-top: 20px;
    }

    .btn {
      padding: 10px 20px;
      background-color: #6c5b7b;
      color: #fff;
      border: none;
      border-radius: 5px;
      text-decoration: none;
      transition: background-color 0.3s ease;
      cursor: pointer;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .btn:hover {
      background-color: #54466d;
    }

    .query {
      color: #fff;
      text-decoration: none;
      transition: color 0.3s ease;
    }

    .query:hover {
      color: #e6e6e6;
    }

    button i{
      margin-right: 8px;
    }

  </style>
</head>
<body>
  <div class="container">
    <h1><i class="fas fa-info-circle"></i>Query Details</h1>
    <div class="issue-details">
      <p><i class="fas fa-user"></i><strong>Username:</strong> {{username}}</p>
      <p><i class="fas fa-phone"></i><strong>Contact Info:</strong> {{contact_info}}</p>
      <p><i class="fas fa-code"></i><strong>Skill Set:</strong> {{skillset}}</p>
      <p><i class="fab fa-github"></i><strong>GitHub ID:</strong> {{github_id}}</p>
      <p><i class="fas fa-link"></i><strong>Repository Link:</strong> <a href={{repo_link}} class="repo-link" target="_blank">{{repo_link}}</a></p>
      <p><i class="fas fa-align-left"></i><strong>Description:</strong> {{description}}</p>

      {{#if user}}
      <form action="/query/respond" method="GET">
        <input type="hidden" name="_id" value={{_id}} required readonly hidden>
        <input type="hidden" name="username" value={{username}} required readonly hidden>
        <div class="btn-container">
          <button type="submit" class="btn"><i class="fas fa-reply"></i>Respond</button>
          <button type="button" class="btn" id="home-btn"><a class="query" href="/"><i class="fas fa-home"></i>Go To Homepage</a></button>
        </div>
      </form>
      {{else}}
      <div class="btn-container">
        <button type="button" class="btn" id="home-btn"><a class="query" href="/"><i class="fas fa-home"></i>Go To Homepage</a></button>
      </div>
      {{/if}}
    </div>
  </div>
</body>
</html>
