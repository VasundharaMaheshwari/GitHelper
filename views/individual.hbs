<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Query Details</title>
  
  <link rel="icon" href="/logo.jpeg" type="image/icon type">

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  
  <!-- Font Awesome for Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

  <style>
    /* Reset and Base Styles */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Roboto', sans-serif;
    }

    body {
      background-image: url("/Home1.jpg");
      background-repeat: no-repeat;
      background-position: center center;
      background-attachment: fixed;
      background-size: cover;
      color: #f0f0f0;
      overflow-x: hidden;
    }

    /* Navbar Styles */
    .navbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 15px 30px;
      background: transparent; 
      position: fixed;
      width: 100%;
      top: 0;
      z-index: 1000;
      transition: background 0.3s ease;
    }

    .navbar:hover {
      background: rgba(0, 0, 0, 0.3); 
    }

    .navbar .logo {
      display: flex;
      align-items: center;
    }

    .navbar .logo img {
      height: 40px;
      margin-right: 10px;
      transition: transform 0.3s ease;
    }

    .navbar .logo img:hover {
      transform: rotate(10deg);
    }

    .navbar .nav-links a {
      display: inline-block;
      padding: 10px 20px;
      margin-left: 15px;
      background-image: linear-gradient(45deg, #6a11cb, #2575fc);
      color: #fff;
      text-decoration: none;
      border-radius: 25px;
      font-size: 16px;
      font-weight: bold;
      transition: background-image 0.3s ease, transform 0.3s ease;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
      position: relative;
    }

    .navbar .nav-links a::after {
      content: '';
      position: absolute;
      width: 0%;
      height: 2px;
      bottom: -5px;
      left: 0;
      background-color: #fff;
      transition: width 0.3s;
    }

    .navbar .nav-links a:hover::after {
      width: 100%;
    }

    .navbar .nav-links a:hover {
      background-image: linear-gradient(45deg, #5d09b1, #1f65e3);
      transform: scale(1.05);
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.5);
    }

    /* Container Styles */
    .container {
      background-color: rgba(0, 0, 0, 0.6); 
      padding: 50px 30px; 
      border-radius: 15px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
      text-align: center;
      max-width: 800px;
      width: 90%;
      margin: 100px auto; 
      backdrop-filter: blur(10px);
      animation: fadeIn 1.2s ease-in-out;
    }

    h1 {
      font-size: 36px;
      margin-bottom: 30px;
      color: #ffffff;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      animation: slideInDown 1s ease-out;
    }

    .issue-details p {
      font-size: 18px;
      margin-bottom: 15px;
      color: #d1d1d1;
      display: flex;
      align-items: center;
      gap: 10px;
      text-align: left;
    }

    .repo-link {
      color: #1e90ff; 
      text-decoration: none;
      font-weight: bold;
      transition: color 0.3s ease;
    }

    .repo-link:hover {
      color: #ffb300;
      text-decoration: underline;
    }

    /* Button Styles */
    .btn-container {
      display: flex;
      justify-content: center;
      gap: 20px;
      margin-top: 30px;
      flex-wrap: wrap;
    }

    .btn {
      padding: 15px 30px;
      background-image: linear-gradient(45deg, #6a11cb, #2575fc);
      color: #fff;
      border: none;
      border-radius: 30px;
      text-decoration: none;
      font-size: 18px;
      font-weight: bold;
      cursor: pointer;
      transition: background-image 0.3s ease, transform 0.3s ease;
      box-shadow: 0 4px 15px rgba(255, 0, 0, 0.3);
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .btn:hover {
      background-image: linear-gradient(45deg, #5d09b1, #1f65e3);
      transform: translateY(-5px);
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.5);
    }

    .query {
      color: #fff;
      text-decoration: none;
      transition: color 0.3s ease;
      display: flex;
      align-items: center;
      gap: 5px;
    }

    .query:hover {
      color: #e6e6e6;
    }

    button i {
      margin-right: 8px;
    }

    /* Animations */
    @keyframes fadeIn {
      from { opacity: 0; transform: scale(0.95); }
      to { opacity: 1; transform: scale(1); }
    }

    @keyframes slideInDown {
      from { transform: translateY(-100px); opacity: 0; }
      to { transform: translateY(0); opacity: 1; }
    }

    /* Responsive Styles */
    @media (max-width: 768px) {
      .navbar {
        flex-direction: column;
      }

      .navbar .nav-links {
        margin-top: 10px;
      }

      .navbar .nav-links a {
        margin: 5px 0;
      }

      .container {
        margin: 120px 20px 40px 20px; 
        padding: 30px 20px;
      }

      h1 {
        font-size: 28px;
      }

      .issue-details p {
        font-size: 16px;
      }

      .btn {
        padding: 12px 25px;
        font-size: 16px;
      }
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <div class="navbar">
    <div class="logo">
      <img src="/logo.jpeg" alt="Git Helper Logo"> 
      <span>Git Helper</span>
    </div>
    <div class="nav-links">
      <a href="/error/faq"><i class="fas fa-question-circle"></i> FAQ</a>
      {{#if user}}
      <a href="/api/user"><i class="fas fa-user-circle"></i> Profile</a>
      {{else}}
      {{/if}}
    </div>
  </div>

  <!-- Container for Query Details -->
  <div class="container">
    <h1><i class="fas fa-info-circle"></i> Query Details</h1>
    <div class="issue-details">
      <p><i class="fas fa-user"></i><strong>Username:</strong> {{username}}</p>
      <p><i class="fas fa-phone"></i><strong>Contact Info:</strong> {{contact_info}}</p>
      <p><i class="fas fa-code"></i><strong>Skill Set:</strong> {{skillset}}</p>
      <p><i class="fab fa-github"></i><strong>GitHub ID:</strong> {{github_id}}</p>
      <p><i class="fas fa-link"></i><strong>Repository Link:</strong> <a href="{{repo_link}}" class="repo-link" target="_blank"><i class="fas fa-external-link-alt"></i> {{repo_link}}</a></p>
      <p><i class="fas fa-align-left"></i><strong>Description:</strong> {{description}}</p>

      {{#if user}}
      <form action="/query/respond" method="POST">
        <input type="hidden" name="issue_id" value={{issue_id}} required readonly>
        <input type="hidden" name="creator" value={{creator}} required readonly>
        <div class="btn-container">
          <button type="submit" class="btn"><i class="fas fa-reply"></i> Respond</button>
          <a href="/#queries" class="btn query"><i class="fas fa-arrow-left"></i> Back</a>
        </div>
      </form>
      {{else}}
      <div class="btn-container">
        <a href="/#queries" class="btn query"><i class="fas fa-arrow-left"></i> Back</a>
      </div>
      {{/if}}
    </div>
  </div>
</body>
</html>

